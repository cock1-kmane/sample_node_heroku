# Language setting
language: node_js

# Version number
node_js:
#  - 0.12
  - 0.10.25

env:
  global:
    - APP_NAME=rc-test
    - FOO=FUBU

  matrix:
    - M1=ONE

build:

  ci:
    - ps -eax
    - ls -al
    
  on_success:
    - git push -f git@heroku.com:$APP_NAME.git master 
  #  - git push -f heroku $BRANCH:master    
 
